namespace TerrainTool.Config
{
    public static class Settings
    {
        // Merge distance for database accumulation (0.001 = 1mm precision)
        public const double MIN_MERGE_DISTANCE = 0.0001;
    }
}
